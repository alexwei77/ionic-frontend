// This should be the right way of dealing with http
// Model it the same as the back end db and export to use
export class Address {
    name: string;
    flat_no: number;
    address: string;
    zip_code: string;
    place_id: string;
    constructor() { }
}